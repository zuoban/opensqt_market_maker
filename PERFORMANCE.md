# 性能优化与复测

2026-09-10，基于 v3.5.13 工作区。基准环境为 Apple M1、macOS arm64、Go 1.27.0；项目最低 Go 版本保持不变。

## 改动

1. **卖价占用扫描**：复用同轮规划已经收集的占价集合；只有订单更新正在执行或版本发生变化时重新扫描。订单更新开始、完成时均推进版本，读取时复核版本，避免更新恰好完成时误用缓存。新建、待确认和待撤销的卖单仍持续占价，最终槽位身份检查保留。
2. **同价盘口去重**：bid/ask 未变且没有等待新盘口重试的订单时，QuoteVersion 单独变化可以跳过重复规划。保存本轮实际使用的盘口，避免吞掉规划期间到达的新报价。重连、盘口变化、成交通知和冷却期限仍会触发调整。
3. **仓位快照**：预分配槽位切片，用已有升序索引线性反转代替排序；在槽位锁外用数值格式化生成展示文本。
4. **面板缓存与广播**：生产快照直接交给缓存，内部只读组装不再复制全部槽位；公开 View 和可能复用缓冲区的其它快照来源继续复制。一次广播只编码一次，多个浏览器共享预编码消息。

## 本机结果

下表为每项两次 200ms 基准的平均值。规划基准使用固定持仓、每轮恢复相同候选 reservation 和模拟执行器，包含规划与未提交结果的处理，不含网络、限流和实盘竞争。

| 场景 | 优化前 | 优化后 | 耗时降低 |
|---|---:|---:|---:|
| 100 个持仓槽位，100 个卖单候选 | 0.418ms | 0.126ms | 70% |
| 1,000 个持仓槽位，100 个卖单候选 | 2.629ms | 0.215ms | 92% |
| 5,000 个持仓槽位，100 个卖单候选 | 14.425ms | 0.669ms | 95% |
| 5,000 个持仓槽位的快照 | 2.606ms | 0.912ms | 65% |

5,000 个持仓槽位的快照单次分配由约 6.03MB 降至 1.41MB（降低约 77%），分配次数由约 30,030 次降至 10,011 次。按 400ms 刷新计算，单快照构建的分配速率由约 15.1MB/s 降至 3.5MB/s；这是累计分配量，不是常驻内存。

缓存内部读取 5,000 个槽位约 25ns、零分配；公开复制视图约 108µs、1.33MB/次。并发测试确认 8 个连接的一次广播仅编码一次。每个连接的网络流量仍然独立。

无新卖单候选时，规划仍需要全槽位扫描，耗时基本不变。实际收益取决于持仓规模、需要补挂的卖单数、盘口变化和浏览器连接数，不代表实盘下单延迟下降相同比例。

## 复测

```sh
go test ./position -run '^$' -bench '^(BenchmarkAdjustOrdersScale|BenchmarkPositionSnapshot)$' -benchmem -benchtime=200ms -count=2
go test ./web -run '^$' -bench '^BenchmarkCachedPositionRead$' -benchmem
go test -race ./...
go build ./...
node --test web/app_frontend_test.js
```

交易回归覆盖并发订单绑定、待确认和待撤卖价占用、同价盘口下的拒单重试，以及规划期间到达的新盘口。面板测试覆盖旧快照在刷新后保持不变、外部视图不能修改缓存、共享编码失败传播、慢连接隔离和多个真实 WebSocket 客户端。

## 保留的安全边界与后续瓶颈

- 提交 lease 继续覆盖交易所调用；它阻止最终状态复检与实际提交之间插入迟到成交修正。不能直接提前解锁。
- 批量提交和对账继续由原协调器串行执行，保持未知结果停止后续批次、优先提交减仓卖单的行为。网络等待与限流仍可能主导实盘延迟。
- `filledOrderKeys` 保存本次运行的完整成交去重历史；自 v3.5.14 起，终态累计进度也保留完整历史，不再按 4,096 条淘汰。因此长期内存增长尚未解决。直接按数量或时间删除会使迟到成交再次记账；需要另行设计精确的持久化去重方案及故障恢复。
- 单一行情源、PostOnly、固定金额网格、只读面板、20 条成交明细和 24 小时汇总均保持原约定。

## 运行期间的只读性能观测（2026-09-11）

程序启动后自动启用，不需要新增配置。读取方式：

- 面板现有事件日志中的 `[性能]` 行，按 `timing.status_print_interval`（分钟）输出 P95、样本数、堆大小、GC 次数、槽位数与成交去重键数；风控暂停期间仍输出。没有样本显示 `--(n=0)`。
- 面板同一地址的 `GET /api/performance`，使用原有 Dashboard Token 鉴权（例如 `X-Dashboard-Token` 请求头）。首次采样前返回 HTTP 503 和 `ready:false`；不支持修改数据的方法。响应使用 `Cache-Control: no-store`。
- `/api/snapshot` 和 WebSocket 的完整快照中新增 `performance` 字段。前端图表不变，仍可从事件日志查看摘要。

后台每秒生成一次快照；接口与广播只读取已经生成的数据，不触发运行时采样、槽位扫描或交易所请求。`sampledAt` 是汇总完成时间，各指标的 `lastObservedAt` 是最近一次成功采样时间，不能把长期无新订单的旧读数当成最近一秒的表现。

### 延迟定义

所有延迟均以毫秒输出。

| 指标 `name` | 起止范围 |
|---|---|
| `adjust_total` | `AdjustOrders` 入口至返回，包含串行锁等待、规划、网络和结果处理；无效价格等提前返回也计数 |
| `adjust_lock_wait` | 调整入口至取得 `adjustMu` 串行锁 |
| `planning` | 准备获取规划锁至释放该锁；包含规划期间的槽位锁等待，不含后续提交与结果处理 |
| `quote_to_planning` | 本轮使用的盘口本机接收时间至准备获取规划锁；只有时间有效且进入规划阶段才记录 |
| `rate_limit_wait` | 每次执行器限流等待，包括取消或失败；批量下单仍按逐订单消耗限流额度，因此一次批量调用可有多次等待样本；不包含交易所拒绝后的重试退避 |
| `place_request` | 一次单笔或原生批量下单的执行边界调用；包含适配器内部处理、响应解析及未知结果确认、提交计时回调和 lease 释放，不是纯网络 RTT；每个批量调用只记一个样本 |
| `cancel_request` | 一次单笔或批量撤单调用；批量失败及其逐笔回退分别记录 |
| `order_update_total` | 仓位管理器订单更新回调入口至退出，包含被忽略的重复消息 |
| `order_update_lock_wait` | `lockMappedSlot` 调用耗时，包含槽位查找、必要的新建和等待槽位锁 |
| `buy_fill_to_sell_submit` | 首次有效 BUY 全成交通知进入仓位管理器，到同槽首次实际提交 SELL 尝试 |
| `sell_fill_to_buy_submit` | 首次有效 SELL 全成交且库存清空的通知进入仓位管理器，到同槽首次实际提交 BUY 尝试 |

`planning` 与 `adjust_total` 存在包含关系，各项 P95 不能相加当成总耗时。`quote_to_planning` 是盘口年龄，其中也可能包含市场没有新报价的时间，并非单独的调度队列延迟。

成交到反向提交覆盖槽位锁等待、调度、规划、限流和本地门禁重试；结束点是**首次提交尝试**，不是交易所接受或挂单成功。远端明确拒绝也会结束本次计时，随后重试不会重复采样。全成交事件只使用本机回调时间，不与交易所时钟相减。跳格分仓把原始 BUY 成交时间复制给接收库存的子槽，每个槽位首次卖出提交各有一个样本。

这两项只覆盖运行期间直接处理到的有效全成交事件。部分成交、启动恢复库存、旧终态修正分支不创建起点；在相反方向首次提交前被回收的槽位不保留样本，也不会为统计阻止回收。尚未补单的等待不会计入已完成样本，不能仅凭较低 P95 判断所有成交都已及时补单。行情窗口变化、主动风控等策略等待同样计入已完成的延迟，不一定是程序性能问题。

### 样本窗口、错误与开销

每项固定保留最近 **1,024 个成功采样的事件**，这是事件数量窗口，不是固定时间窗口。P50/P95/P99 使用 nearest-rank，均值、最大值与最后值均来自该窗口；低样本数时百分位参考价值有限。`count` / `errors` / `droppedSamples` 为本次进程运行的累计值；程序重启归零，采样数据不持久化。

`errors` 按边界返回结果计数：调整看最终返回错误，限流看等待错误，下单看返回错误/空订单/批量结果缺失或任一成员失败，撤单看原始适配器错误（包括最终被执行器容忍的“订单不存在”）。其余时延指标仅计时，`errors` 为 0。本地门禁拒绝不会增加下单调用次数；这些计数不是交易成功率或业务失败率。

热点路径使用固定数组、`TryLock` 和内存计数，不执行网络、文件或日志 I/O；统计锁忙则立即丢弃本次性能样本，增加 `droppedSamples`。采样丢弃不影响订单事件或去重。丢弃样本不计入 `count/errors`，高竞争时百分位可能有偏差，需同时查看丢弃量。11 项延迟的原始采样数组合计约 88 KiB，不随成交数量增长；每个槽位另外保存少量成交关联元数据。后台复制样本后在统计锁外排序。

复测采样与满窗口后台汇总开销：

```sh
go test ./telemetry -run '^$' -bench '^(BenchmarkObserve|BenchmarkObserveSince|BenchmarkRefresh)$' -benchmem -benchtime=500ms -count=2
```

本机 Apple M1 / Go 1.27.0 两次基准：单次 `Observe` 约 52–54ns，包含起止计时的 `Start` + `ObserveSince` 约 112ns，均为 0 B / 0 次堆分配。11 项满窗口后台 `Refresh` 约 127–145µs，约 3.2 KiB / 4 次分配，每秒执行一次。这是采样器的无竞争微基准，不代表整轮交易延迟；高竞争路径会跳过样本。

### 内存与 GC

`runtime` 使用 Go `runtime/metrics`，不强制 GC：

- `heapObjectsBytes`：当前堆对象占用；`heapLiveBytes`：最近一次 GC 的存活堆；`heapGoalBytes`：当前 GC 堆目标。它们均不是操作系统 RSS。
- `totalAllocatedBytes`：累计堆分配量，间隔两次读数取差可估算分配速率；不能用它表示常驻内存。
- `gcCycles` / `goroutines`：累计 GC 周期数与当前协程数。
- `gcPauseP99MsUpperBound`：Go 运行时生命周期暂停直方图的 P99 桶上界，不是最近 1,024 个交易样本或精确暂停时长。仅当 `gcPauseQuantileReady:true` 时有效；`gcPauseSamples` 表示该直方图样本数。

`state` 只读取容器长度，不遍历槽位或执行对账，各计数分别一致但不是同一交易事务快照。`filledDedupKeys` 是成交去重表键数，当前按 ClientOrderID 优先、缺失时按订单 ID 生成一个键；`filledOrders` 是累计完成订单数。`terminalOrderKeys`、`pendingAbsenceKeys` 是对应缓存的当前条目数。

建议先在正常运行中收集多个状态周期，优先确认下单调用、限流、回调锁等待和规划耗时的相对占比，再决定后续改动。成交去重键的长期增长问题仍需单独设计精确持久化方案。

成交去重持久化的设计与故障验证现见 [FILL_LEDGER_DESIGN.md](FILL_LEDGER_DESIGN.md)。独立存储原型已验证去重标记与模型状态的原子提交，尚未替换生产内存表；后续仍须验证真实槽位恢复和磁盘同步开销。
