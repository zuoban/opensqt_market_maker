# 性能优化与复测

## 2026-09-18：减少成交回调中的查询和日志参数分配

成交去重与终态进度查询使用局部 64 字节缓冲区构造原格式键，并仅在 map 查找表达式中转换为字符串。常规 ClientOrderID（含 Binance 允许的 36 字节长度）及纯数值 OrderID 查询无需堆分配；超长兼容身份仍走原表示。ClientOrderID 优先级、`client:` / `order:` 名字空间、经纪商前缀归一化与全部入表记录保持不变，没有采用概率去重或淘汰键。

回调诊断在准备格式化价格和可变参数前检查日志等级，避免最终被过滤的日志仍产生分配。会计增量先发布，日志过滤仅作用于输出；正常 INFO 成交/盈亏、WARN 非法进度和 DEBUG 重放诊断保留原有等级及内容。

本轮修改前后各在独立进程执行一次相同百万订单场景，修改前已包含最近 20 条记录缓冲区复用。Apple M1 / macOS arm64 / Go 1.27.1 / GOMAXPROCS=8：

| 指标 | 全成交：修改前 → 后 | 终态：修改前 → 后 |
|---|---:|---:|
| 工作循环累计分配 | 0.600 → 0.276 GiB（-54%） | 1.056 → 0.508 GiB（-52%） |
| 工作循环分配次数 | 30,516,035 → 11,016,131 | 48,015,901 → 16,516,141 |
| 工作循环自然 GC 次数 | 36 → 16 | 40 → 18 |
| 工作循环耗时 | 2.42 → 2.07 s | 3.72 → 3.17 s |
| GC 后新增堆 | 83.73 → 83.71 MiB | 158.46 → 158.27 MiB |

全成交场景 300 万次回调，终态场景 500 万次；完整去重与终态记录仍分别为百万条。内存采样中，历史查询和被过滤的诊断参数不再成为分配来源，入表键及 map 增长仍保留。

**测量边界**：以上回放日志级别为 ERROR、telemetry 关闭，计时含事件生成、断言及回收，排除检查点强制 GC、报告和 profile 写盘。默认 INFO 仍输出有效成交/盈亏日志，因此整场回放的降幅不能直接套到默认实盘服务。为验证默认等级下的独立收益，另测每次一组成交/终态查询和一次被过滤的 DEBUG 重放诊断（各 250ms）：

| 微基准 | 修改前 | 修改后 |
|---|---|---|
| 常规 ClientOrderID 查询一组 | 62.9ns、64 B、2 次分配 | 39.9ns、0 B、0 次分配 |
| 36 字节 ClientOrderID 查询一组 | 70.0ns、96 B、2 次分配 | 40.7ns、0 B、0 次分配 |
| 数值 OrderID 查询一组 | 166.6ns、80 B、4 次分配 | 63.4ns、0 B、0 次分配 |
| INFO 等级下被过滤的 DEBUG 重放日志 | 46.4ns、40 B、3 次分配 | 20.9ns、0 B、0 次分配 |

128 字节兼容身份继续分配（每组 288 B、2 次），本次约 91 → 103ns；没有宣称任意长度身份均无分配。单次本机计时不是实盘尾延迟测量。长期历史堆占用基本不变，持久化与运行容量问题尚未解决。

验证覆盖 ClientOrderID 优先、两个名字空间不串用、36/57/58 字节边界、超长和含 Unicode/NUL 的身份、int64 两端数值；原百万回放覆盖重复、迟到修正与槽位重建。新增 INFO/WARN/ERROR 等级下的日志和会计检查。已通过 `go test -race ./...`、`go build ./...`、两个百万场景及差异格式检查。

复测：

```sh
rtk proxy go test -race ./...
rtk proxy go build ./...
rtk proxy go test ./position -run '^$' -bench '^(BenchmarkOrderHistoryLookup|BenchmarkSuppressedOrderReplayLog)$' -benchmem -benchtime=250ms
rtk proxy env OPENSQT_HISTORY_STRESS_ORDERS=1000000 OPENSQT_HISTORY_PROFILE_DIR=/tmp/opensqt-callback-after-filled go test ./position -run '^TestOrderHistoryStress/filled$' -count=1 -v
rtk proxy env OPENSQT_HISTORY_STRESS_ORDERS=1000000 OPENSQT_HISTORY_PROFILE_DIR=/tmp/opensqt-callback-after-terminal go test ./position -run '^TestOrderHistoryStress/terminal$' -count=1 -v
```

## 2026-09-18：复用最近 20 条成交记录的缓冲区

仓位管理器预分配 20 条记录的容量；列表满后，在原 `filledOrdersMu` 锁内向前移动 19 条记录并覆盖末尾，保持旧到新的内部顺序。`Snapshot` 继续在读锁内复制为独立的最新优先列表，历史快照和外部消费者的修改互不影响。全成交去重表、累计成交数、24 小时汇总及通知路径沿用原逻辑。

同一 Apple M1 / macOS arm64 / Go 1.27.1 / GOMAXPROCS=8，修改前后各在独立进程执行一次相同的百万全成交订单场景（300 万次回调），本轮结果：

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 工作循环累计分配 | 3.461 GiB | 0.601 GiB |
| 工作循环分配次数 | 31,517,066 | 30,516,048 |
| 工作循环自然 GC 次数 | 226 | 36 |
| 自然 GC 累计暂停 | 12.95 ms | 1.97 ms |
| 工作循环耗时 | 2.92 s | 2.42 s |
| GC 后新增堆 | 83.69 MiB | 83.82 MiB |

累计分配减少约 **82.7%**，内存采样中原来每笔复制 20 条切片的分配来源已消失。回放总耗时本次降低约 17%，属于单次本机观测，不是实盘延迟承诺。工作循环包括夹具、断言及回收成本，日志为 ERROR、telemetry 关闭；GC 计数不含检查点主动触发的 GC。完整负载定义见 [HISTORY_STRESS_REPORT.md](HISTORY_STRESS_REPORT.md)。

**收益边界**：减少临时分配和 GC 压力。百万个去重键仍完整保留，GC 后历史占用基本不变；持久化及长期容量问题继续保留。内部每笔最多移动 19 条记录，对外快照仍有独立复制的成本。

回归新增覆盖空列表、未满、刚满、持续溢出时的完整顺序，旧快照独立、消费者修改隔离、被移出列表的旧成交仍去重，以及并发写入/读取。百万回放同时验证成交通知、累计会计和同价槽位回收重建。已通过 `go test -race ./position ./web`、`go build ./...` 和差异格式检查。

复测：

```sh
rtk proxy go test -race ./position ./web
rtk proxy go build ./...
rtk proxy env OPENSQT_HISTORY_STRESS_ORDERS=1000000 OPENSQT_HISTORY_PROFILE_DIR=/tmp/opensqt-recent-fills-after go test ./position -run '^TestOrderHistoryStress/filled$' -count=1 -v
rtk proxy go tool pprof -list appendFilledOrder -alloc_space /tmp/opensqt-recent-fills-after/filled.heap.pprof
```

## 2026-09-18：百万订单历史压力测试

新增可复现的离线生命周期压力测试，完整数据和命令见 [HISTORY_STRESS_REPORT.md](HISTORY_STRESS_REPORT.md)。本轮只增加测试与报告，未改动生产交易逻辑。

Apple M1 / macOS arm64 / Go 1.27.1，固定一个活跃槽位，分别运行百万个唯一全成交订单和百万个唯一终态订单。GC 后新增堆约 **83.8 MiB / 158.4 MiB**，确认增长主要来自完整成交去重键与终态进度。两场景分别处理 300 万、500 万次回调；重复、乱序、迟到数量/盈亏修正及 1,953 次同价槽位回收重建均通过断言。`resolvedAbsentOrders` 在最多 64 个未完成记录的百万次循环中每批归零，该结论不代表任意突发下存在硬上限。

初次压测时，百万全成交场景累计分配约 **3.46 GiB**，内存采样约 **83%** 来自最近 20 条成交列表的反复切片复制。该临时分配问题已由上方缓冲区复用优化处理；历史 map 的持续增长仍需持久化方案解决，不能直接淘汰去重证据。

大规模测量使用显式环境变量开启，普通测试保持较小规模。本轮三个百万规模场景与 `go test -race ./position` 已通过；数字来自关闭常规日志和 telemetry 的加速回放，不代表线上吞吐或回调尾延迟。

## 2026-09-18：确认查询不再占用提交锁

此前一次执行器下单调用会持有槽位 lease，直到 Binance 适配器返回；遇到不确定响应时，这包含创建请求、按 ClientOrderID 查询、可能的同 ID 重试。订单流回调是顺序派发的，一个槽位在确认查询期间等锁，还可能延后后面的其它订单事件。

现在 Binance 在**每次创建 HTTP 请求返回后**释放对应 lease，再进行只读确认。原生批次会先释放本批所有槽位；参数校验失败的成员也先释放。只读确认挂起时，同槽成交回调和只读快照可以取得槽位锁，不必等待确认结果。

同 ID 重试重新消耗限流额度、获取槽位锁，并复核库存、原新单代际和交易健康状态。若确认期间迟到成交使请求失效，保留原 reservation，返回 UNKNOWN 并停止该代际后续创建。UNKNOWN 中包含的“槽位失效”错误不会被批次聚合逻辑吞掉。首次提交前失效仍沿用原清理行为；新保护标记不依赖 telemetry 是否开启。

验证使用本地 HTTP 服务主动挂起确认查询，分别覆盖单笔和原生批量：确认未返回时所有相关槽位已能拿锁；确认不存在后，重试仍用同一 ClientOrderID，并在重新取得 lease 后发送。另覆盖参数无效成员释放、健康/限流/取消/代际复检，以及真实执行器与仓位管理器贯通下的全成交、迟到库存修正和迟到 REST 回包。测试不连接真实交易所。

**收益范围**：减少不确定结果确认与重试等待造成的额外锁占用；普通创建请求的网络等待仍持锁。协调器仍等待确认完成后才开始下一轮，因此本轮不代表下单 RTT 或所有成交到补单延迟下降。没有解除最终槽位复检到实际创建之间的互斥，也未接入成交去重持久化。

本轮完整验证已通过：`go test -race ./...`、`go build ./...`、38 项前端测试。

复测：

```sh
go test ./ ./order ./exchange/binance ./position -run 'Test(SubmissionBoundary|CreateBoundary|BatchLocalValidation|Confirmation|GapStackRetryAfterConfirmation)' -count=1
go test -race ./...
go build ./...
node --test web/app_frontend_test.js
```

## 2026-09-18：分批调度与补单等待观测

生产路径现在每次 `AdjustOrders` 只规划、预留并执行一个批次：Binance 每批最多 5 笔，不再按距离盘口远近拆批。不支持原生批量的执行器每轮一笔；未声明批次大小的兼容/测试执行器保留原行为。未轮到的槽位不创建 reservation。

成功处理本批结果后，若仍有候选，清除同格跳过指纹并在释放调整锁后通知原协调器继续。下一轮重新检查健康状态、最新行情与库存，优先规划 ReduceOnly 卖单。未知结果仍保留原订单身份并进入门禁恢复；明确拒单使用原冷却通知，不因续批绕过冷却。提交 lease、严格 PostOnly、订单配额及固定金额规则保留。

离线测试使用虚拟网络时间：买单窗口 20 格，其中一格已有买单，19 笔深度买单待挂；每批 5 笔，首批请求过半时向另一个已挂单槽位注入 BUY 全成交，盘口不变。比较从本机处理该成交到下一次 SELL 提交的调度等待：

| 每批模拟耗时 | 原整窗口连续提交 | 每批交还协调器 |
|---|---:|---:|
| 100ms | 350ms | 50ms |
| 300ms | 1,050ms | 150ms |
| 1,000ms | 3,500ms | 500ms |

此场景由等待剩余 3.5 批降为 0.5 批，**不是实盘测量或普遍收益承诺**。不包含真实限流、网络抖动、重试、撤单、对账、系统调度或同槽提交锁竞争。交易请求仍串行，每批重新扫描也会增加整窗口本地规划成本；本轮改善成交响应机会，不提高交易所吞吐上限。同槽回调仍可能等待当前提交的网络返回。

同时新增 `adjust_request_wait`、`reconcile_total` 和待确认对向买/卖单的数量、最长等待，定义见下文。状态日志增加 `[补单等待]`；首次提交被拒绝后，待确认等待仍持续，避免只看已结束样本的 P95 漏掉积压。

本轮 Apple M1 / macOS arm64 两次 250ms 微基准：13 项满窗口 `Refresh` 约 0.247–0.258ms/次，约 3.5 KiB、4 次分配；5,001 个均有等待标记的槽位，状态计数约 0.048–0.054ms/次、零分配。两者生产均每秒一次。单次 `Observe` 约 73–82ns，含起止计时约 172–173ns，均零分配；数字受本机负载影响，不与下方历史测量作严格前后对照。

回归覆盖：分批不预留尾部、价格不变时继续挂满窗口、真实协调循环与生产适配器贯通、不同价格共用原生批次、续批使用最新行情、卖单优先、拒单冷却、UNKNOWN 身份保留、门禁/退出阻止续批；观测覆盖请求合并与冷却排除、拒单后 REST/WS 受理、错误身份、重复成交、库存修正、槽位回收及只读 API 缓存。

本轮验证已通过：`go test -race ./...`、`go build ./...` 和全部 38 项前端测试；未连接实盘交易验证。

复测：

```sh
go test ./position -run 'Test(Adjustment|BatchScheduling|BatchYield|Telemetry)' -count=1
go test ./ -run 'Test(OneBatchScheduling|BatchContinuation|AdjustQueue|AdapterForwards)' -count=1
go test ./telemetry ./position -run '^$' -bench '^(BenchmarkObserve|BenchmarkObserveSince|BenchmarkRefresh|BenchmarkTelemetryPendingStateCounts)$' -benchmem -benchtime=250ms -count=2
go test -race ./...
go build ./...
node --test web/app_frontend_test.js
```

## 2026-09-18：扫描、面板、日志与对账等待

基于 Go v3.5.23 工作区，未改变交易门禁、提交 lease、PostOnly、固定金额、成交去重规则或单一行情源。

- **槽位扫描**：有序索引直接保存槽位指针，省去每次扫描的逐价 `sync.Map.Load`。成员视图仍以写时复制发布；回收旧槽时标记 retired，槽位状态继续由原有锁保护，同价重建与提交前身份复检保留。复杂度仍是 O(N)，索引增删仍需复制数组。
- **面板空闲**：首次启动保留一份快照，连续 10 秒无读取后暂停仓位快照扫描；再次读取唤醒唯一后台刷新任务，接口立即返回缓存及真实年龄。持续订阅时按配置间隔刷新，不因 HTTP/WS 请求数量增加扫描任务。
- **面板传输**：协商 WebSocket permessage-deflate，压缩等级 BestSpeed，同轮广播共享压缩帧。非压缩客户端、HTTP 快照和前端数据格式保持兼容；压缩减少网络传输，不减少 JSON 编码与浏览器解析的数据量。
- **日志**：控制台与 DEBUG 文件分别通过容量 4,096 的队列输出。文件 I/O 锁与队列锁分离，入队/关闭互斥，消除慢输出拖住交易回调及并发关闭 channel 的风险。队列饱和会丢弃输出并计数，面板最近日志仍同步记录；性能状态日志报告累计丢弃数。退出排空队列，FATAL 同步输出。
- **对账**：完成 pending 收敛后，持仓与挂单只读查询并行执行，共享原有截止时间；失败取消另一查询。正常网络等待由两次请求耗时之和降为两者较大值加调度开销。协调器、本地校验及恢复门禁保持串行；两次 REST 读取依然不是交易所原子快照。

Apple M1 / macOS arm64；优化前为本次分析的单次 250ms 基准，优化后为两次 250ms 基准均值。以下均不含真实交易所请求、限流及并发交易负载。

| 场景 | 优化前 | 优化后 | 耗时降低 |
|---|---:|---:|---:|
| 5,000 个持仓槽位，无新卖单候选 | 0.537ms | 0.218ms | 约 59% |
| 5,000 个持仓槽位，100 个卖单候选 | 0.644ms | 0.320ms | 约 50% |
| 5,000 个持仓槽位的快照 | 1.038ms | 0.740ms | 约 29% |

快照构建分配量仍约 1.41MB/次；活跃面板的这部分 GC 压力没有消除，空闲时通过停止扫描避免继续产生。缓存只读访问因记录活跃时间和唤醒判断，从约 25ns 增至 68–70ns，仍为零分配。

同一 5,000 持仓槽位夹具的 JSON 为 2,588,828 字节；BestSpeed DEFLATE 后约 48,624 字节，压缩耗时约 1.11ms/帧。此项是原始 DEFLATE 微基准，不含 WebSocket 帧头、网络、JSON 编码和浏览器解压；不同数据的压缩比会变化。真实 WebSocket 回归覆盖压缩协商、共享广播和不支持压缩的客户端。

新增回归覆盖旧索引视图与同价重建、慢输出下的非阻塞入队、并发关闭日志队列、空闲缓存唤醒、对账查询并行和失败取消。完整竞态检查、构建和前端测试均需通过。

复测命令：

```sh
go test ./position ./web -run '^$' -bench 'Benchmark(AdjustOrdersScale|PositionSnapshot|CachedPositionRead)$' -benchmem -benchtime=250ms -count=2
go test ./position -run '^$' -bench '^BenchmarkPositionSnapshotDeflate$' -benchmem -benchtime=250ms
go test -race ./...
go build ./...
node --test web/app_frontend_test.js
```

**尚未解决**：实际创建请求跨网络导致的同槽回调等待，以及完整成交/终态历史的长期内存增长。整窗口连续串行提交已由上方的逐批调度替代，额外的只读确认锁等待也已分离，但单次创建网络等待和限流仍存在。没有在创建请求返回前释放锁或淘汰去重键；这些需要独立的提交状态机及持久化恢复设计。下面保留此前优化的历史说明及当前观测指标定义。

## 2026-09-10 基线

2026-09-10，基于 v3.5.13 工作区。基准环境为 Apple M1、macOS arm64、Go 1.27.0；项目最低 Go 版本保持不变。

## 改动

1. **卖价占用扫描**：复用同轮规划已经收集的占价集合；只有订单更新正在执行或版本发生变化时重新扫描。订单更新开始、完成时均推进版本，读取时复核版本，避免更新恰好完成时误用缓存。新建、待确认和待撤销的卖单仍持续占价，最终槽位身份检查保留。
2. **同价盘口去重**：bid/ask 未变时，QuoteVersion 单独变化可以跳过重复规划，拒单冷却由协调器定时唤醒。保存本轮实际使用的盘口，避免吞掉规划期间到达的新报价。重连、盘口变化、成交通知和冷却期限仍会触发调整。
3. **仓位快照**：预分配槽位切片，用已有升序索引线性反转代替排序；在槽位锁外用数值格式化生成展示文本。
4. **面板缓存与广播**：生产快照直接交给缓存，内部只读组装不再复制全部槽位；公开 View 和可能复用缓冲区的其它快照来源继续复制。一次广播只编码一次，多个浏览器共享预编码消息。

## 本机结果

下表为每项两次 200ms 基准的平均值。规划基准使用固定持仓、每轮恢复相同候选 reservation 和模拟执行器，包含规划与未提交结果的处理，不含网络、限流和实盘竞争。

| 场景 | 优化前 | 优化后 | 耗时降低 |
|---|---:|---:|---:|
| 100 个持仓槽位，100 个卖单候选 | 0.418ms | 0.126ms | 70% |
| 1,000 个持仓槽位，100 个卖单候选 | 2.629ms | 0.215ms | 92% |
| 5,000 个持仓槽位，100 个卖单候选 | 14.425ms | 0.669ms | 95% |
| 5,000 个持仓槽位的快照 | 2.606ms | 0.912ms | 65% |

5,000 个持仓槽位的快照单次分配由约 6.03MB 降至 1.41MB（降低约 77%），分配次数由约 30,030 次降至 10,011 次。按 400ms 刷新计算，单快照构建的分配速率由约 15.1MB/s 降至 3.5MB/s；这是累计分配量，不是常驻内存。

缓存内部读取 5,000 个槽位约 25ns、零分配；公开复制视图约 108µs、1.33MB/次。并发测试确认 8 个连接的一次广播仅编码一次。每个连接的网络流量仍然独立。

无新卖单候选时，规划仍需要全槽位扫描，耗时基本不变。实际收益取决于持仓规模、需要补挂的卖单数、盘口变化和浏览器连接数，不代表实盘下单延迟下降相同比例。

## 复测

```sh
go test ./position -run '^$' -bench '^(BenchmarkAdjustOrdersScale|BenchmarkPositionSnapshot)$' -benchmem -benchtime=200ms -count=2
go test ./web -run '^$' -bench '^BenchmarkCachedPositionRead$' -benchmem
go test -race ./...
go build ./...
node --test web/app_frontend_test.js
```

交易回归覆盖并发订单绑定、待确认和待撤卖价占用、同价盘口下的拒单重试，以及规划期间到达的新盘口。面板测试覆盖旧快照在刷新后保持不变、外部视图不能修改缓存、共享编码失败传播、慢连接隔离和多个真实 WebSocket 客户端。

## 保留的安全边界与后续瓶颈

- 提交 lease 继续覆盖每次实际创建请求；它阻止最终状态复检与实际提交之间插入迟到成交修正。创建请求返回后的只读确认不持锁，同 ID 重试重新限流并获取/复检 lease；不能在实际创建请求返回前直接解锁。
- 每批提交和对账继续由原协调器串行执行，每批后重新规划，保持未知结果停止后续批次、优先提交减仓卖单的行为。网络等待与限流仍可能主导实盘延迟。
- `filledOrderKeys` 保存本次运行的完整成交去重历史；自 v3.5.14 起，终态累计进度也保留完整历史，不再按 4,096 条淘汰。因此长期内存增长尚未解决。直接按数量或时间删除会使迟到成交再次记账；需要另行设计精确的持久化去重方案及故障恢复。
- 单一行情源、PostOnly、固定金额网格、只读面板、20 条成交明细和 24 小时汇总均保持原约定。

## 运行期间的只读性能观测（2026-09-11）

程序启动后自动启用，不需要新增配置。读取方式：

- 面板现有事件日志中的 `[性能]` 行，按 `timing.status_print_interval`（分钟）输出 P95、样本数、堆大小、GC 次数、槽位数与成交去重键数。没有样本显示 `--(n=0)`。
- 面板同一地址的 `GET /api/performance`，使用原有 Dashboard Token 鉴权（例如 `X-Dashboard-Token` 请求头）。首次采样前返回 HTTP 503 和 `ready:false`；不支持修改数据的方法。响应使用 `Cache-Control: no-store`。
- `/api/snapshot` 和 WebSocket 的完整快照中新增 `performance` 字段。前端图表不变，仍可从事件日志查看摘要。

后台每秒生成一次快照；接口与广播只读取已经生成的数据，不触发运行时采样、槽位扫描或交易所请求。`sampledAt` 是汇总完成时间，各指标的 `lastObservedAt` 是最近一次成功采样时间，不能把长期无新订单的旧读数当成最近一秒的表现。

### 延迟定义

所有延迟均以毫秒输出。

| 指标 `name` | 起止范围 |
|---|---|
| `adjust_total` | `AdjustOrders` 入口至返回，包含串行锁等待、规划、网络和结果处理；无效价格等提前返回也计数 |
| `adjust_lock_wait` | 调整入口至取得 `adjustMu` 串行锁 |
| `planning` | 准备获取规划锁至释放该锁；包含规划期间的槽位锁等待，不含后续提交与结果处理 |
| `quote_to_planning` | 本轮使用的盘口本机接收时间至准备获取规划锁；只有时间有效且进入规划阶段才记录 |
| `rate_limit_wait` | 每次执行器限流等待，包括取消或失败；批量下单仍按逐订单消耗限流额度，适配器内部同 ID 重试也重新限流，因此一次批量调用可有多次等待样本；不包含交易所拒绝后的重试退避 |
| `place_request` | 一次单笔或原生批量下单的执行边界调用；包含适配器内部处理、响应解析及未知结果确认、提交计时回调和 lease 释放，不是纯网络 RTT；每个批量调用只记一个样本 |
| `cancel_request` | 一次单笔或批量撤单调用；批量失败及其逐笔回退分别记录 |
| `order_update_total` | 仓位管理器订单更新回调入口至退出，包含被忽略的重复消息 |
| `order_update_lock_wait` | `lockMappedSlot` 调用耗时，包含槽位查找、必要的新建和等待槽位锁 |
| `buy_fill_to_sell_submit` | 首次有效 BUY 全成交通知进入仓位管理器，到同槽首次实际提交 SELL 尝试 |
| `sell_fill_to_buy_submit` | 首次有效 SELL 全成交且库存清空的通知进入仓位管理器，到同槽首次实际提交 BUY 尝试 |
| `adjust_request_wait` | 最早就绪的显式调整请求至协调器取走并开始调整；合并请求不重置起点，延迟请求从冷却到期算起，含门禁恢复/对账等待；不覆盖行情频道或回调入口之前的积压 |
| `reconcile_total` | `Reconcile` 入口至返回，包含对账串行锁等待、pending 收敛、并行持仓/挂单查询与本地校验；失败也记录 |

`planning` 与 `adjust_total` 存在包含关系，各项 P95 不能相加当成总耗时。`quote_to_planning` 是盘口年龄，其中也可能包含市场没有新报价的时间，并非单独的调度队列延迟。

成交到反向提交覆盖槽位锁等待、调度、规划、限流和本地门禁重试；结束点是**首次提交尝试**，不是交易所接受或挂单成功。远端明确拒绝也会结束本次计时，随后重试不会重复采样。全成交事件只使用本机回调时间，不与交易所时钟相减。每个持仓槽位首次卖出提交各有一个样本，不再合并或拆分跳格库存。

这两项只覆盖运行期间直接处理到的有效全成交事件。部分成交、启动恢复库存、旧终态修正分支不创建起点；在相反方向首次提交前被回收的槽位不保留样本，也不会为统计阻止回收。尚未补单的等待不会计入已完成样本，需结合下方待确认数量与最长等待，不能仅凭较低 P95 判断所有成交都已及时补单。行情窗口变化等策略等待同样计入延迟，不一定是程序性能问题。

`state.pendingOppositeBuys` / `pendingOppositeSells` 是有效全成交后仍未确认对向订单受理的当前槽位数；`oldestOppositeBuyWaitMs` / `oldestOppositeSellWaitMs` 是对应最长本机等待，无等待时为 0。与首次提交样本不同，这些标记跨越首次尝试、明确拒单及 UNKNOWN；仅有效、匹配身份的 NEW/部分成交/全成交或有效 REST NEW 绑定确认后清除。下一次全成交可开始相反方向的新等待。

统计单位是槽位。槽位被回收、库存修正使该方向不再适用时移除标记；不保留历史欠单，也不阻止策略回收。起点范围与首次提交指标一致；全成交通知尚未取得槽位锁并完成处理时，等待数量尚不可见，处理后年龄会包含这段锁等待。行情窗口和普通拒单冷却等待也计入年龄。

### 样本窗口、错误与开销

每项固定保留最近 **1,024 个成功采样的事件**，这是事件数量窗口，不是固定时间窗口。P50/P95/P99 使用 nearest-rank，均值、最大值与最后值均来自该窗口；低样本数时百分位参考价值有限。`count` / `errors` / `droppedSamples` 为本次进程运行的累计值；程序重启归零，采样数据不持久化。

`errors` 按边界返回结果计数：调整和对账看最终返回错误，限流看等待错误，下单看返回错误/空订单/批量结果缺失或任一成员失败，撤单看原始适配器错误（包括最终被执行器容忍的“订单不存在”）。其余时延指标仅计时，`errors` 为 0。本地门禁拒绝不会增加下单调用次数；这些计数不是交易成功率或业务失败率。

热点路径使用固定数组、`TryLock` 和内存计数，不执行网络、文件或日志 I/O；统计锁忙则立即丢弃本次性能样本，增加 `droppedSamples`。采样丢弃不影响订单事件或去重。丢弃样本不计入 `count/errors`，高竞争时百分位可能有偏差，需同时查看丢弃量。13 项延迟的原始采样数组合计约 104 KiB，不随成交数量增长；每个槽位另外保存少量成交关联元数据。有效全成交发布一个不可变的等待标记，确认或回收后可被 GC，不累计历史。后台复制样本后在统计锁外排序。

复测采样与满窗口后台汇总开销：

```sh
go test ./telemetry -run '^$' -bench '^(BenchmarkObserve|BenchmarkObserveSince|BenchmarkRefresh)$' -benchmem -benchtime=500ms -count=2
```

此前 11 项指标的本机 Apple M1 / Go 1.27.0 基准：单次 `Observe` 约 52–54ns，包含起止计时的 `Start` + `ObserveSince` 约 112ns，均为 0 B / 0 次堆分配；满窗口后台 `Refresh` 约 127–145µs，约 3.2 KiB / 4 次分配。它们是无竞争微基准，当前 13 项指标及待确认遍历应以上方复测命令为准，不代表整轮交易延迟；高竞争路径会跳过样本。

### 内存与 GC

`runtime` 使用 Go `runtime/metrics`，不强制 GC：

- `heapObjectsBytes`：当前堆对象占用；`heapLiveBytes`：最近一次 GC 的存活堆；`heapGoalBytes`：当前 GC 堆目标。它们均不是操作系统 RSS。
- `totalAllocatedBytes`：累计堆分配量，间隔两次读数取差可估算分配速率；不能用它表示常驻内存。
- `gcCycles` / `goroutines`：累计 GC 周期数与当前协程数。
- `gcPauseP99MsUpperBound`：Go 运行时生命周期暂停直方图的 P99 桶上界，不是最近 1,024 个交易样本或精确暂停时长。仅当 `gcPauseQuantileReady:true` 时有效；`gcPauseSamples` 表示该直方图样本数。

`state` 每秒读取容器长度，并遍历一次有序槽位索引的原子等待标记，不获取槽位锁或执行对账；各计数分别一致但不是同一交易事务快照。HTTP/WS 仅取后台缓存，不额外扫描。`filledDedupKeys` 是成交去重表键数，当前按 ClientOrderID 优先、缺失时按订单 ID 生成一个键；`filledOrders` 是累计完成订单数。`terminalOrderKeys`、`pendingAbsenceKeys` 是对应缓存的当前条目数。

建议先在正常运行中收集多个状态周期，优先确认下单调用、限流、回调锁等待和规划耗时的相对占比，再决定后续改动。成交去重键的长期增长问题仍需单独设计精确持久化方案。

成交去重持久化的设计与故障验证现见 [FILL_LEDGER_DESIGN.md](FILL_LEDGER_DESIGN.md)。独立存储原型已验证去重标记与模型状态的原子提交，尚未替换生产内存表；后续仍须验证真实槽位恢复和磁盘同步开销。

2026-09-18 的 [账本百万历史离线验收与优化](FILL_LEDGER_ACCEPTANCE_REPORT.md) 已完成复测：启动累计分配约 3.81 GiB → 214 MiB（减少 94.5%），同轮打开时间 14.59 → 13.46 秒；精确重试 P99 43.20 → 0.07 ms，已消除空事务写盘。新事务同步提交 P99 仍约 45–48 ms，约 99.5% 的计时落在数据库写入与同步阶段，下一步优先验证目标磁盘的延迟预算。校验后的缓存查询 P99 约 0.026 ms；这些结果不代表实盘延迟或完整恢复后的内存，账本仍未接入生产。

部署磁盘可运行 `python3 scripts/ledger_acceptance.py --directory /mnt/data`：自动执行离线故障/恢复检查，在指定磁盘测量同步提交与启动开销，并输出基于逐笔耗时的排队估算。原子存储门禁及阻塞超时已通过真实执行器的离线验证，生产接线仍未完成。参数、报告格式和估算边界见同一验收报告。
