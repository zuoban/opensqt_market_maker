# 成交账本原型：百万历史离线验收

日期：2026-09-18。范围：`internal/fillledger` 格式 2 槽位事务，以及 `position` 的测试用真实成交计算/发布适配器。生产版本仍为 v3.5.23，账本未接入运行路径。

**后续优化已完成复测：百万历史启动累计分配降低 94.5%，精确重试不再写盘；新事务同步提交尾延迟仍未改善。** 下文保留首轮基线，优化前后的同参数对照见“剖析与优化复测”。

## 首轮验收结论（优化前）

百万条历史下，精确查询、累计记账、重启后重复回放和旧终态修正通过本轮离线验证。**尚不满足生产接入条件**：同步提交尾延迟和启动全量校验已有明显成本，完整状态恢复、存储故障停单、迁移与历史缺口补齐仍未实现。

- 百万历史的新事务同步提交 P95 **36.40 ms**、P99 **44.74 ms**，本轮最大 **124.91 ms**；不能继续用此前小规模约 8–10 ms 的均值估算回调占锁成本。
- 新进程打开账本耗时 **20.43 秒**，随后读取 1,000 个已知槽位耗时 **7.62 ms**。两遍全量校验是优先分析的启动路径。
- 启动累计分配 **3.81 GiB**，发生 **1,185 次 GC**。这是累计分配量；GC 后 Go 堆约 **311 KiB**，两者不能混为常驻内存。
- 校验后的历史命中查询 P99 **0.0278 ms**。该结果包含一个槽位和一个订单的读取，系统页面缓存未清空，不能称为冷查询。
- 百万历史数据库表观文件大小 **1.72 GiB**。它不把所有历史键常驻 Go map，但 mmap、系统缓存和完整交易状态仍需单独测量。

## 环境与方法

Apple M1、16 GiB 内存、macOS 27.0（26A428）、Go 1.27.1、darwin/arm64、GOMAXPROCS=8、bbolt v1.5.0，本机临时目录。性能数据未启用 race；故障回归另行使用 race。测试期间没有连接交易所，没有读取真实配置或运行主程序。

每个规模建立独立临时数据库，结束自动删除。所有数据库保持默认同步语义，`NoSync=false`。

| 项目 | 定义 |
|---|---|
| 预填充历史 | 1,000 / 10,000 / 100,000 / 1,000,000 条订单，固定 1,000 个槽位 |
| 存储载荷 | 每槽 1,024 字节、每订单 256 字节的不透明 DTO；不是完整生产状态模式 |
| 历史组成 | BUY/SELL 交替，一半 FILLED、一半 CANCELED/EXPIRED/REJECTED；精确十进制累计量与盈亏 |
| 数据准备 | 测试内部按每批 1,000 条同步预填充；使用与公开 Commit 相同的规范化记录、事务摘要与封装 |
| 预填充一致性 | 32 条夹具与 32 次公开同步 Commit 分别写入、重新打开并比较记录、统计、修订号 |
| 新事务 | 每个规模追加 1,000 笔真实同步 Commit，各事务变更一个槽位、一条订单与累计统计 |
| 精确重试 | 另计 1,000 次相同事务 Commit；断言不改变最终统计或修订号 |
| 启动 | 关闭父进程数据库，由新子进程 OpenSlots；随后按每批至多 64 个已知键读取所有槽位 |
| 查询 | 打开及槽位读取完成后，各执行 1,000 次确定性伪随机订单命中/未命中，并读取对应槽位 |
| 百分位 | nearest-rank；单轮单写者测试，无并发交易负载，P99 不是延迟上界 |

预填充耗时不计入逐笔提交延迟，也不能当作逐笔落盘吞吐。本轮四个规模预填充分别约 0.03 / 0.25 / 2.74 / 37.52 秒；完整规模测试约 174.57 秒。查询与恢复所见订单数为预填充数加 1,000，因此第一档实际查询 2,000 条历史。

新进程不等于冷磁盘：父进程刚完成写入，且 OpenSlots 本身完整扫描全部记录两次。没有清空系统缓存或执行冷启动设备测试。

## 同步提交与查询

单位均为 ms，每项 1,000 个样本。规模为预填充订单数。

| 历史规模 | 新事务 P50 | P95 | P99 | 最大 | 精确重试 P95 | 精确重试 P99 |
|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 8.97 | 11.20 | 15.09 | 50.69 | 9.99 | 13.10 |
| 10,000 | 7.85 | 10.01 | 14.07 | 31.99 | 9.68 | 12.54 |
| 100,000 | 9.98 | 13.07 | 16.03 | 24.96 | 13.33 | 18.39 |
| 1,000,000 | 31.92 | 36.40 | 44.74 | 124.91 | 30.92 | 37.06 |

首轮版本的存储层精确重试仍提交 bbolt 写事务；百万规模该项最大值为 108.34 ms，后续优化已去除这次空提交。真实回放适配器会先 Read，再让状态计算器忽略已处理事件；它与直接重试 Commit 是不同路径。这个业务判断仍需保留，因为比最后事务更早的事件不等于存储层的精确重试。

| 历史规模 | 查询命中 P95 | 命中 P99 | 未命中 P95 | 未命中 P99 |
|---:|---:|---:|---:|---:|
| 1,000 | 0.0214 | 0.0295 | 0.0125 | 0.0165 |
| 10,000 | 0.0208 | 0.0304 | 0.0122 | 0.0181 |
| 100,000 | 0.0229 | 0.0316 | 0.0119 | 0.0167 |
| 1,000,000 | 0.0225 | 0.0278 | 0.0128 | 0.0216 |

命中查询分布于整个历史，未命中 ID 位于已知最大 ID 之后。没有模拟交易写入竞争、缓存驱逐或随机损坏；存储错误不能作为未命中处理，这由独立故障用例验证。

## 启动、内存与磁盘

| 历史规模 | 打开耗时 | 已知槽位读取 | 启动累计分配 | GC 次数 | 采样堆峰值 | GC 后堆 |
|---:|---:|---:|---:|---:|---:|---:|
| 1,000 | 73.62 ms | 8.58 ms | 15.63 MiB | 4 | 3.21 MiB | 284.49 KiB |
| 10,000 | 207.42 ms | 7.35 ms | 50.82 MiB | 15 | 3.61 MiB | 285.30 KiB |
| 100,000 | 1.54 s | 7.43 ms | 400.94 MiB | 121 | 3.62 MiB | 290.58 KiB |
| 1,000,000 | 20.43 s | 7.62 ms | 3,898.51 MiB | 1,185 | 4.66 MiB | 311.23 KiB |

`openLedgerDB` 先只读全量校验，再取得写锁后全量复核；每遍都检查页面及槽位/订单封装、规范化身份、计数与修订号。这两遍有不同安全用途，不能为了启动速度直接删除一遍。下一步应剖析 JSON 解码、十进制规范化、校验和与页面检查各自的分配/CPU 占比，评估在保留损坏检测和写锁后复核的前提下减少重复工作。

内存指标来自整个子进程 Go runtime，启动累计分配包含打开、已知槽位读取及测量开销。堆峰值每 10 ms 采样，可能漏掉短峰；GC 后堆测量时数据库保持打开。槽位读取只验证并丢弃 DTO，**没有构建和保留完整生产内存模型**；不能把约 311 KiB 当作程序恢复后的总内存，更不能把它等同 RSS。没有加载完整历史键集合。

| 历史规模 | 预填充文件大小 | 追加与修正后文件大小 | 追加后逻辑数据库大小 | 新提交循环分配/笔 | 循环 GC 次数 |
|---:|---:|---:|---:|---:|---:|
| 1,000 | 8.00 MiB | 8.00 MiB | 6.77 MiB | 73.25 KiB | 24 |
| 10,000 | 33.16 MiB | 33.16 MiB | 22.45 MiB | 100.59 KiB | 33 |
| 100,000 | 189.88 MiB | 189.88 MiB | 179.17 MiB | 113.63 KiB | 41 |
| 1,000,000 | 1,756.97 MiB | 1,756.97 MiB | 1,746.27 MiB | 126.31 KiB | 47 |

文件大小为 `os.Stat().Size()`，不是物理占用或备份容量；逻辑大小为 `tx.Size()`，在子进程三次旧订单修正之前记录。追加 1,000 条未触发文件扩容，不表示历史存储停止增长。百万档按本夹具折算约 1.84 KB/历史订单，包含 JSON/校验封装、索引与空闲空间；不能直接外推到真实 DTO 或任意碎片化状态。

“新提交循环分配”包含输入生成与结果断言，因此不是纯 Commit 的分配基准。数据库使用 mmap，RSS、驻留映射页和系统缓存未做完整峰值测量；生产容量规划必须补测。

## 故障与正确性验收

| 场景 | 本轮验证 |
|---|---|
| 百万历史重启 | 精确累计统计与修订号正确；按已知键读取所有 1,000 个槽位，随机历史查询正确 |
| 最早终态数量修正 | 修正最早的 BUY、SELL 终态，随后重复提交；仅补记一次数量与盈亏 |
| 旧 SELL 的纯盈亏补差 | 数量不变、版本更新，允许负向盈亏补差；精确重试不重复记账 |
| 最早全成交重放 | 历史已经增长并发生修正后，重试最早 FILLED 仍返回当前统计 |
| 新增真实状态崩溃用例 | 旧 BUY 数量补差、旧 SELL 数量补差、旧 SELL 纯盈亏补差，均在槽位已绑定另一新 SELL 后执行 |
| 三个崩溃位置 | 提交前、同步成功但尚未发布、发布后直接退出子进程；3 类修正 × 3 个位置，共 9 个场景通过 |
| 重启回放断言 | 完整槽位/订单进度/统计仅为提交前或提交后状态；重复修正、旧终态、旧 NEW 不覆盖新绑定、不重复通知或记账 |
| 既有故障套件 | 空间不足、短写、权限、I/O 类错误；事务回滚、已提交但响应丢失；读取失败、实例故障锁存、损坏拒绝恢复 |

大规模用例使用不透明存储载荷；真实成交计算、成本释放和内存发布由小规模 `position` 回放/崩溃用例验证，两者是互补证据。写入错误由事务边界包装器注入，进程崩溃使用 `os.Exit` 跳过 Close/defer；不代表真实磁盘写满、内核故障或主机掉电已验收。

## 剖析与优化复测

同日、同机器、同数据及采样参数，分别运行优化前后独立编译的测试程序。新对照的优化前百万启动为 14.59 秒，首轮为 20.43 秒，说明时间读数受运行环境影响；下表使用这轮配对结果，不把两轮基线混算。系统缓存条件仍与首轮相同。

### 已实施的改动

- **精确重试回滚空事务。** 在原互斥与同一数据库事务中核验事务摘要、读取当前状态后，回滚未修改的事务，避免空提交的页面及同步写入。真实新事务仍同步成功后返回；旧部分成交仍由调用方读取进度后重新计算。
- **复用启动扫描对象。** 复用封装载荷缓冲、临时订单及可复用的状态字节容量，减少逐行分配；每行先清空所有字段，状态缓冲长度归零，缺失字段不能继承上一行。公开读取仍返回独立字节数据。
- **减少重复校验分配。** 解码得到的订单不再克隆一次 State，不再使用反射比较字段；十进制校验复用原规范字符串，避免 Split 和无变化的字符串拼接。槽位桶句柄在同一校验事务中复用。

文件格式、SHA-256 验证、页面检查、两遍完整记录校验、修订与身份校验均保留；没有关闭同步或淘汰历史。错误仍锁存不可用，精确重试的内部回滚信号若附带 I/O 错误也不会被当作成功。

### 结果

| 历史规模 | 打开前 → 后 | 启动累计分配前 → 后 | 精确重试 P99 前 → 后 |
|---:|---:|---:|---:|
| 1,000 | 74.65 → 45.62 ms | 15.63 → 8.21 MiB | 12.9857 → 0.1877 ms |
| 10,000 | 204.78 → 163.47 ms | 50.83 → 10.29 MiB | 13.6953 → 0.1551 ms |
| 100,000 | 1,508.93 → 1,341.06 ms | 400.92 → 29.14 MiB | 18.0531 → 0.1258 ms |
| 1,000,000 | 14.59 → 13.46 s | 3,898.45 → 214.25 MiB | 43.1991 → 0.0700 ms |

百万档启动分配减少 **94.5%**，启动 GC **1,183 → 65 次**，打开时间减少 **7.7%**。十万记录的独立 OpenSlots 基准两轮也得到：分配约 **413.3 → 27.3 MB/次**，耗时 **1.48–1.50 → 1.32–1.34 秒/次**。该微基准不包含追加的 1,000 条订单或已知槽位读取，所以数值与规模测试不同。

百万档精确重试 P50/P95/P99 为 **0.0306 / 0.0365 / 0.0700 ms**，最大 0.1474 ms；bbolt 写入及同步时间为零。回归测试进一步确认数据库事务编号不变、写次数为零，返回的仍是最新累计状态，且结果字节可以由调用方独立修改。

**新事务 P99 为 45.00 → 48.25 ms，没有确认改善。** 优化后 P50/P95 为 29.96 / 40.95 ms，最大 152.86 ms，仍有明显尾延迟。优化前 1,000 笔新事务合计 31.629 秒，其中数据库写入与同步 31.468 秒，占 **99.49%**；优化后对应 30.748 / 30.588 秒，占 **99.48%**。该计时来自 bbolt TxStats，包含页面写入、同步及元数据写入/同步，不是纯 fsync 或纯设备延迟。

剩余启动 CPU 样本主要落在双层 JSON 解码。分配大幅降低不意味着全量扫描成本已经消失；继续降低启动时间需要评估记录编码或解码路径，并保留同等完整性验证。当前数据库大小与基线相同；百万档查询命中 P99 为 0.0261 ms，缓存条件及 Go 堆/RSS 的限制均沿用上文。

### 回归验证

`go test -race ./internal/fillledger ./position -count=1`、项目构建和百万规模回放全部通过，主程序依赖图仍不包含账本。新增回归覆盖空事务无写盘、回滚信号不能吞掉 I/O 错误、复用缓冲不能掩盖缺失字段，以及返回订单数据独立持有。十进制规范化用独立语法/有理数校验完成 **918,611 次**模糊测试，保留精度、符号、零与长度约束。

分配结果针对当前 Go 1.27.1 的 JSON 实现；其他 Go 版本的缓冲复用和分配开销应重新测量。性能剖析不包含真实并发交易负载，也未验证 Linux/Windows 的磁盘与内存行为。

## 下一步及接入门槛

1. **优先验证目标部署磁盘的同步提交预算。** 当前新事务耗时几乎都在数据库写入与同步阶段；在目标磁盘测量尾延迟、冷缓存、RSS、回调锁等待与积压。启动后续重点为 JSON 解码与全量扫描。保留同步成功后发布的协议，不通过关闭同步、解除必要槽位锁或删除历史键降低数字。
2. **完善生产状态模式与全部写路径。** 覆盖网格锚点、精度、成本、跳格多槽事务、reservation、UNKNOWN 身份和 ClientOrderID 序号；补齐真实启动枚举及内存模型恢复。
3. **接通已经过离线验证的存储健康停单条件。** 原子门禁、独立超时与执行器测试现已通过；生产协调器仍须在每次新单前组合健康条件，监听故障通知，保留有界事件队列并收敛在途操作。同步超时不代表事务取消。
4. **验证恢复连续性与迁移。** 先恢复槽位，再有界缓冲订单流、补齐停机/断线历史并对账；缺失账本、旧备份回滚、缓冲溢出或无法证明历史覆盖均保持停单。现有部署不能直接创建空账本替代迁移。
5. **完成目标平台和部署验收。** Linux/Windows 的文件锁、目录同步、持久卷与崩溃测试，备份恢复及容量告警；当前单机 macOS 测量不能替代这些工作。

上述门槛通过后，才能评估生产替换 `filledOrderKeys` / `terminalOrders`。当前交付包括存储原型、离线测试和验收工具，没有引入生产运行依赖或配置开关，测试持久文件仅在独立临时目录创建。

## 存储故障门禁与部署脚本

2026-09-18 追加离线验证，新增 `GuardedSlots` 包装器；仍未接入主程序。验证内容：

| 场景 | 结果 |
|---|---|
| 读取错误、写入前失败、事务回滚、提交结果丢失 | 故障锁存，后续读写被拒绝，错误不返回可发布状态 |
| 持有真实数据库互斥时阻塞读取/写入 | 独立计时器仍触发故障，Health 不等待数据库锁 |
| 超时后磁盘成功返回 | 原实例继续故障；迟到结果不发布，重开账本核实实际提交结果 |
| 单笔、逐笔回退、原生批量 BUY/SELL | 限流期间发生的故障在最终复检被拦截，无交易所创建调用 |
| 普通门禁再次启用 | 仍被存储故障拦截；单笔/批量保护性撤单可执行 |
| 只读确认期间故障 | 同 ID 再次创建被阻止，继续保留 UNKNOWN 与原 ClientOrderID |
| 已落盘但返回阻塞的真实成交 | 不发布内存持仓、累计成交或补单通知；重启恢复只记账一次 |
| 旧计时器、完成/超时竞争 | 不污染后续操作，不恢复已失败的健康状态 |

包装器一次只接收一个操作；`ErrStorageBusy` 要求调用方保留事件并按序重试。尚未实现生产事件缓冲、启动门禁、故障协调与关机接线。已发出或已经通过复检的远端请求仍需按在途/UNKNOWN 协议处理，不能承诺本地故障能撤回远端订单。

### 一键运行

需要 Python 3 标准库、项目要求的 Go 版本和已准备好的依赖；默认正确性检查启用 race，需要平台支持及相应编译工具链。指定**已经存在的目标数据盘目录**，脚本只创建自己独占的子目录，测试数据库结束后自动清理，保留报告和日志。默认百万规模要求约 3.05 GiB 可用数据盘空间，Go 构建缓存另计。

```sh
python3 scripts/ledger_acceptance.py --directory /mnt/data

# 可先快速检查环境。
python3 scripts/ledger_acceptance.py --directory /mnt/data --orders 1000 --samples 100

# 使用自行确定的提交延迟预算及预计到达间隔。
python3 scripts/ledger_acceptance.py --directory /mnt/data \
  --p99-budget-ms 50 --arrival-ms 20 50 100 --queue-capacity 256
```

这里的 50 ms、到达间隔和队列容量只是参数示例，不是推荐的交易 SLA。平台不支持 race 时可显式传 `--no-race`，报告会记录已关闭检查，不会静默跳过。

脚本先在本机临时目录编译测试程序，再把**运行时临时目录**设置到目标磁盘；因此数据库、故障及崩溃夹具落在目标文件系统，测试程序本身可在数据卷 `noexec` 时运行。它仅执行固定的离线测试，不启动 main、不加载真实交易配置、不连接交易所。编译依赖未缓存时，Go 可能需要先下载依赖。

每次运行创建 `opensqt-ledger-acceptance-*` 目录，包含 `REPORT.md`、完整 `results.json`、正确性检查日志及 `scale.log`。退出码：0 为测量完成，1 为执行/验证失败，2 为参数错误或显式 P99 预算超限（运行后的报告用 `budget_exceeded` 区分），130 为中断；测量完成不是生产准入结论。

排队结果使用按实际发生顺序导出的逐笔提交耗时，对固定到达间隔建立单写者 FIFO 模型。模型不丢弃事件，计算最大等待数、等待 P99 和首次容量超限；它**不是实际并发回调或队列压力测试**，没有计入回调读取、业务锁、网络等额外成本，也不能替代生产缓冲验收。

本机验证：账本、执行器、仓位模块的 race 测试通过；脚本按 1,000 历史、100 样本完整试跑并生成报告，临时数据库已清理。另以故意过低的预算验证返回 `budget_exceeded` 和退出码 2，排队模型的空闲、饱和及超容量边界检查通过。此处未新增百万性能结论；前面的百万数据仍为上一轮对照结果。

## 复测

本轮 `go test -race ./internal/fillledger ./position -count=1`、`go build ./...` 均通过；主程序依赖图确认不包含 `internal/fillledger` 或 bbolt。

```sh
# 默认测试不会生成百万数据库。
go test -race ./internal/fillledger ./position -count=1
go build ./...

# 本轮规模与采样参数；各规模顺序运行，临时数据库自动删除。
OPENSQT_LEDGER_SCALE_ORDERS=1000000 OPENSQT_LEDGER_SCALE_SAMPLES=1000 \
  go test ./internal/fillledger -run '^TestSlotLedgerScale$' -count=1 -v -timeout=20m

# 单独运行新增的 9 个真实状态崩溃场景。
go test ./position -run '^TestRealTerminalCorrectionCrashPreservesNewBinding$' -count=1

# 十万历史的启动剖析；CPU/分配总 profile 包含预填充，按 OpenSlots 筛选。
go test ./internal/fillledger -run '^$' -bench '^BenchmarkSlotLedgerOpenScale$' \
  -benchtime=1x -count=2 -cpuprofile=/tmp/ledger-open.cpu \
  -memprofile=/tmp/ledger-open.mem -o /tmp/ledger-profile.test
go tool pprof -top -cum -focus=OpenSlots /tmp/ledger-profile.test /tmp/ledger-open.cpu
go tool pprof -top -alloc_space -focus=OpenSlots /tmp/ledger-profile.test /tmp/ledger-open.mem

# 十进制语法与精度的独立随机校验。
go test ./internal/fillledger -run '^$' -fuzz '^FuzzLedgerDecimalNormalization$' \
  -fuzztime=20s -parallel=2
```

规模测试输出的 `ledger_scale` JSON 包含原始耗时、分配、文件大小与恢复结果，便于在目标机器比较。测试参数允许的预填充规模为 1,000–1,000,000，采样次数为 100–5,000。设计与生产接入约束见 [FILL_LEDGER_DESIGN.md](FILL_LEDGER_DESIGN.md)。
