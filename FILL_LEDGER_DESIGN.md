# 成交去重持久化：设计与故障验证

日期：2026-09-11。基于 `f42e1c2` 的交易流程。

**本阶段交付的是存储事务原型和故障测试。生产交易尚未接入；`filledOrderKeys` 仍保留原行为，长期内存增长尚未解决。** 原型位于 `internal/fillledger/`，没有被 `main`、`position`、`order` 或面板导入，不创建真实运行数据文件。

## 结论与决策

采用 **bbolt 精确磁盘索引 + 与业务状态同事务提交** 的方向。仅持久化“这个订单已经处理过”的标记不足以安全恢复：标记和它对应的持仓、成本、累计成交进度必须一起恢复。写入失败一律按结果不确定处理，锁存存储不可用状态；生产接入时应同步禁止所有新订单，恢复前重新打开账本、校验并完成对账。

原型固定使用 `go.etcd.io/bbolt v1.5.0`，纯 Go，支持项目 Go 1.25.4；默认同步写入，不启用 `NoSync`、异步落盘或定时批量确认。新增依赖不会进入当前主程序的运行依赖图。

| 方案 | 决策依据 |
|---|---|
| 给内存去重表加 TTL / 数量上限 | 迟到重放仍可能发生，删除身份会失去精确去重能力 |
| Bloom filter 等概率结构 | 假阳性会漏记真实成交，只能将来作为可验证的查询加速层 |
| 文本日志 + 启动时恢复全部键 | 可以保存历史，但仍把全部键加载到堆，且需要自行解决半条写入、索引与原子更新 |
| bbolt B+tree | 按完整键精确查询，事务包含去重与状态，保持跨平台无 CGO；接受单写者与同步磁盘延迟 |
| SQLite | 同样可行，但当前没有 SQL 查询需求，选择较小的键值事务接口；将来报表不应强迫交易账本承担分析负载 |

## 当前代码里的恢复缺口

1. `position.SuperPositionManager.OnOrderUpdate` 在槽位锁内先检查 `wasFilledOrderRecorded`，再通过 `applyOrderExecutionDelta` 修改库存、成本和累计盈亏，最后调用 `recordFilledOrder` 写入内存去重表。
2. 先写磁盘标记、后改内存：标记成功后崩溃，重启会忽略这笔成交，但内存中的库存变化已经丢失。先改内存、后写标记：故障时又无法证明是否可以安全重放。单纯调整两行调用顺序无法解决。
3. `main.go` 先启动订单流，再调用 `Initialize`。初始化从交易所总持仓与平均开仓价分配新槽位，并没有恢复原槽位身份、旧锚点、活动订单与累计成交进度。因此不能把旧的“已处理”标记直接接到新分配的槽位上。
4. `safety.Reconciler` 验证持仓及活动订单，不是历史成交重放引擎。净持仓一致也不能证明逐槽成本、盈亏、终态修正都已处理一次。
5. 当前 `filledOrderKey` 优先使用 ClientOrderID。`utils.GenerateOrderID` 使用秒级时间和进程内序号；同秒重启存在复用可能。Binance 经纪商前缀还会在不同回包中出现或消失。跨进程身份不能仅依赖这个字符串。
6. 部分成交及 CANCELED / EXPIRED / REJECTED 的迟到修正有独立累计进度；只解决 FILLED 的标记不能宣称所有订单更新都已具备跨重启恰好一次记账能力。

## 持久身份与数据模型

一个账本绑定一个 `Scope`：交易所、mainnet/testnet、稳定的非秘密账户引用、交易对、策略状态版本。文件内容内保存同样的范围，打开时必须完全匹配。API Key 及其哈希均不能作为账户身份，避免轮换密钥后创建另一份去重历史；账户引用与真实账户的一致性仍需接入时验证。

全成交主键使用 Binance `OrderID`，作用域由账本提供。ClientOrderID 保留作身份核验，并统一移除已知经纪商前缀。相同 OrderID 的方向、累计数量、累计报价金额或规范化 ClientOrderID 冲突时停止处理，不能任选一份覆盖。不同 OrderID 复用同一 ClientOrderID 不视为重复。

原型要求 OrderID 为正；没有远端 ID 的确认需要先查询并绑定真实身份。尚未拿到 ID 的 reservation 在生产设计中使用独立 `pending_client` 映射，不得把“按 ClientOrderID 识别到消息”当作持久全成交身份已确认。完整成交的时间戳不作为去重键；重放时间变化不产生新身份。

原型的累计数量与金额使用有限长度的正十进制字符串，规范化前导/尾随零，不接受 NaN、Inf、负数或指数。真正接入必须保留交易所十进制精度，不能先转 float64 再假装恢复原始精度。

| 原型记录 | 内容与约束 |
|---|---|
| `meta/header` | 格式版本 1、完整 Scope；未知版本拒绝打开 |
| `meta/checkpoint` | 修订号、调用方提供的完整模型状态；原型限制 1 MiB |
| `completed/{OrderID}` | 规范化全成交证据、首次提交修订号；不按时间或数量淘汰 |
| 记录封装 | JSON 原始载荷及 SHA-256；校验身份、修订范围和载荷完整性 |

每次原型事务恰好增加一个完成订单，因此启动还验证记录数等于 checkpoint 修订号。JSON 与校验和用于验证协议，后续生产格式必须另设版本；不要直接把原型数据库当作正式运行目录。

生产状态模式还必须覆盖：

- 网格锚点、价格/数量精度、网格间距及不可兼容配置指纹。
- 每槽数量、真实成本、订单身份、累计已成交数量/金额、已释放成本、累计盈亏与订单绑定状态。
- 订单终态的单调累计进度；活动部分成交、撤单后迟到修正与独立盈亏修正的版本水位。
- 跳格父子关系及一次拆分涉及的所有槽位，保证多槽变更一起落盘。
- 已保留/待确认订单、已分配的 ClientOrderID 序号。生成新身份后、调用交易所前先保存 reservation；UNKNOWN 不更换身份重下。
- 会计累计值与运行会话标识。持久会计和当前进程的面板统计分别定义，继续遵守最近 20 条明细、24 个小时桶的展示约定。

真实生产事务应按受影响槽位增量写入。原型每次保存完整的小模型，是为验证崩溃边界；将全部槽位在每个回调中序列化，会引入新的性能瓶颈，不能直接采用。

## 事务协议

1. 在现有身份、累计进度和槽位约束下，读取修订号并生成**尚未发布的状态变更**。这个过程不能下单、通知补单或修改对其他协程可见的会计状态。
2. 一个同步事务检查全成交主键。相同证据已存在时返回当前 checkpoint，不使用调用方提交的旧投影；存在冲突时锁存不可用。
3. 未处理的新成交要求 `expectedRevision` 与当前修订一致，否则返回修订冲突，由调用方重新读取、计算。不得将旧投影直接覆盖新状态。
4. 同事务写入完成订单证据、相关状态及新修订号；等待 bbolt 提交并同步成功。
5. 成功后才发布返回的状态并通知调整订单。重复事件返回的是**最新整体状态**，不能恢复为该笔老成交当时的历史 checkpoint。
6. 任何存储读取错误不能当作“未见过订单”。写入/同步错误无法证明提交是否发生，返回结果中不携带可发布状态，并锁存 `ErrUnavailable`。后续查询与提交均失败，直到显式关闭并重新打开。

| 中断位置 | 重启后允许的状态 | 重放动作 |
|---|---|---|
| 提交前或事务未提交 | 旧状态且无该完成标记 | 完整处理一次 |
| 持久提交后、内存发布前 | 新状态及完成标记同时存在 | 恢复新状态，忽略重复作用 |
| 发布之后 | 新状态及完成标记同时存在 | 同上 |
| API 返回写入/同步错误 | 旧状态或新状态均可能 | 保持停单，重新打开并读取确认，不能在原实例盲目重试 |

这提供本地持久状态的原子性，不使交易所下单和本地事务成为同一个事务。外部订单接受后丢失应答，仍走现有 UNKNOWN、ClientOrderID 查询、订单流和对账流程。

## 生产接入顺序与停单条件

以下是后续实现方案，当前未修改这些交易路径。

1. 加载配置并关闭新单门禁。先打开与校验账本，恢复旧锚点、槽位、进度和 pending 身份；恢复路径不得再调用“按总仓位重新分配槽位”的初始化逻辑。
2. 启动订单流，在恢复/补齐期间放入**有界缓冲区**。缓冲溢出、连接断代或事件缺口均使启动失败或保持停单；交易事件不能像性能样本一样丢弃后继续运行。
3. 对已知活动、pending 和尚未收敛终态查询权威订单状态，使用累计数量/金额做单调更新。查询订单不等于轮询价格，不新增价格流。断线期间交易所不保证重放全部消息，因此必须设计历史缺口补齐；超出可查询历史、出现未识别订单或无法证明覆盖范围时保持停单。
4. 重放缓冲更新，通过同一个持久处理入口，再完成完整持仓、订单、成本与进度验证。各健康条件成立后才允许 `PlaceOrder`。
5. 运行期存储故障作为新的、锁存的交易健康条件。`SetSubmissionHealthGuard` 在实际交易所调用前检查它，不能仅依赖 200ms 健康轮询；所有新增买卖订单都停用，保护性撤单仍可执行。
6. 故障通知在业务锁释放后交给协调器，禁止持有槽位锁时同步重入撤单/对账。对账成功不能自行清除存储故障；需要存储恢复和账本校验也通过。
7. 退出时先停止新单及协调器，收敛已接收的交易更新和必要撤单结果，再关闭账本；关闭失败记录为存储故障。面板故障仍不得影响交易，账本却属于交易关键依赖。

接入时需逐条审计所有写路径：订单流、REST 回包、pending 解析、迟到终态修正、跳格分仓、reservation 创建/失败清理、槽位回收。只改 `recordFilledOrder` 不满足设计。

锁策略：先获得本次变更需要的业务锁并生成纯状态投影，再开始短数据库事务；数据库事务内不获取新的业务锁、不访问交易所、不通知协调器。保持 submission lease 覆盖最终复检与远端调用。同步磁盘等待会增加回调锁占用，必须利用现有指标及新增存储耗时测量后评估，不能先解除槽位锁来换取速度。

原型的 `Health` 使用实例互斥，只供存储协议验证。正式提交门禁应读取独立的原子故障标记，不能在持有 lease 时等待数据库互斥；阻塞不返回的磁盘 I/O 还需要独立超时观察器锁存停单，超时不等价于写入已取消。

## 初始化、迁移与损坏恢复

`Create` 与 `Open` 明确区分。创建使用 `O_EXCL` 和 0600 文件权限，并同步父目录；恢复时文件缺失、空文件、截断、记录校验失败、版本或 Scope 不匹配都不回退到空数据库。初始化失败保留残留文件，后续不自动覆盖。

原型打开先只读检查文件高水位、数据库页面、记录校验、身份与计数，再取得排他写锁后复核。第二个写者被拒绝。目录同步不受支持的系统会返回错误，不能把不支持当作成功。

现有部署第一次切换需要单独的停单迁移：固定旧会话，确认远端挂单与累计执行状态，迁移完整槽位和去重历史并通过对账后再切换。当前进程丢失之前的去重身份，无法仅从一个净持仓数字重建；旧进程已结束、历史覆盖不全时，保持停单并走人工核验的新基线流程。**缺失账本不等价于首次安装。** 迁移工具与正式配置开关本阶段没有实现。

检测到损坏时保留原文件、停止交易并离线诊断。从一致备份恢复后还要证明备份后的订单更新已经补齐；不能仅因文件可打开就恢复下单。版本升级使用独立的新文件及显式迁移验证，不原地猜测修复。

校验和不能证明账本是最新的：整个旧备份、双元页回退到较旧的有效事务、存储设备丢失已确认写入，都可能留下内部一致但过时的数据库。生产需要恢复代际/备份来源记录及足够的权威订单历史来验证连续性；不能证明时保持停单。SHA-256 也不是防篡改认证。测试不能替代目标磁盘、文件系统、容器卷的掉电验证。

## 内存、磁盘与部署边界

磁盘记录保留完整精确身份，当前原型没有内存键缓存和删除接口，查询走 B+tree。该选择避免将每个去重键都装进 Go map，**不代表 RSS 或整体堆为固定上限**：bbolt 的 mmap、文件缓存、空闲页管理，以及启动页面检查使用的内存都可能随数据库增长。原型启动做两次校验，成本随页数/记录数增长，应在生产规模下量化。

磁盘历史仍持续增长；监控磁盘余量和事务耗时。压缩只回收空闲页，不删除去重语义。后续归档必须继续支持对所有历史键的精确查询，不能将归档后的查询返回视为“未处理”。

未来正式路径建议单独配置持久目录，Docker 使用持久卷，并同时从 Git、镜像构建上下文和发行包排除数据库/备份。配置只存非秘密账户引用和目录，不写 API Key。原型测试全部使用临时目录，不改现有配置与部署。

本次验证环境为 macOS arm64、Go 1.27.0。Linux 本地文件系统需补运行验证；Windows 的目录同步、文件锁和损坏恢复尚未验证，正式支持前需要等价的持久创建实现与平台测试。网络文件系统及不承诺同步语义的卷不在当前保证范围。

## 已实现的故障测试

| 测试组 | 验证内容 |
|---|---|
| 正常重启与重复消息 | 持仓数量、成本、成交数只增加一次；经纪商前缀和十进制等价写法一致；旧重复消息返回最新状态 |
| 身份与并发 | ClientOrderID 复用不混淆不同远端订单；20 个并发重复提交只有一个新提交；旧修订不能覆盖新状态 |
| 写入结果不确定 | ENOSPC、短写、权限、I/O 同步类错误，分别覆盖写入前、事务回滚、提交成功但结果丢失；当前实例锁存不可用 |
| 读取与关闭失败状态 | 查询错误不变成未命中；关闭后不作为空账本提供服务 |
| 子进程直接退出 | 提交前、事务内、持久提交后发布前、发布后四个位置跳过所有 defer 和 Close，重启检查原子性 |
| 文件与数据异常 | 缺失、空文件、截断数据页、状态/完成记录校验失败、缺失记录/桶、身份或修订冲突、未知版本；拒绝打开并保留文件 |
| 历史规模 | 5,000 个历史记录重启后，最早及跨越 1,024/4,096 边界的订单仍可精确查询与去重 |

写入故障通过真实 bbolt 事务外的测试包装器注入，覆盖提交与回滚两种可能结果；没有伪称真实磁盘已满或模拟内核断电。子进程 `os.Exit` 验证进程突然退出，不是主机掉电。模型包含数量、成本和成交数，用于证明存储边界；不是完整 `SuperPositionManager` 的恢复测试。

复测：

```sh
go test -race ./internal/fillledger -count=1
go test -race ./...
go mod verify
go build ./...
```

本机验证结果：存储包 44 项测试（含子用例）通过；全量 race 检测共 807 项测试通过，构建及模块校验通过。主程序依赖图确认没有引入 `internal/fillledger` 或 bbolt，当前交易运行路径未改变。

## 正式接入的验收条件

- 用实际仓位状态与纯状态变更器取代小模型，覆盖部分成交、累计均价、盈亏补差、跳格多槽原子性、迟到终态、重连乱序和停机成交。
- 完成存储健康门禁及真实执行器测试：持久化失败后，包括原生批量路径在内都不能提交新订单；保护性撤单与 UNKNOWN 收敛继续可用。
- 启动恢复不重新分配已持久槽位，缓冲溢出/历史缺口/对账不一致一律拒绝放行。
- 迁移、备份回滚、文件丢失、版本升级与损坏恢复都有可重复执行的验收流程。
- 在目标 Linux/macOS/Windows 文件系统及实际规模下测量同步提交 P95/P99、冷查询、启动检查、Go 堆、RSS 和磁盘增长，并完成所支持平台的崩溃测试。

这些条件完成前，保留当前生产内存去重实现，不能仅替换成磁盘 `Lookup/Commit` 就宣称已消除长期风险。
